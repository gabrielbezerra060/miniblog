{
    "compilerOptions": {
        "allowJs": true, // Allow JavaScript files
        "checkJs": false, // Optional: Disable type checking for JS files
        "outDir": "./dist",
        "rootDir": "./",
        "noEmit": true // Prevent TypeScript from emitting files
    },
    "include": ["src/**/*", "playwright.config.ts"],
    "exclude": ["node_modules", "src/tests", "e2e", "playwright"]
}